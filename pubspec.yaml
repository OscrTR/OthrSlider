name: othr_slider
description: "A highly customizable range slider."
version: 0.0.1
homepage: https://github.com/OscrTR/OthrSlider

environment:
  sdk: ^3.5.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
